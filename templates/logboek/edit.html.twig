{% extends 'base.html.twig' %}

{% block title %}Edit Logboek{% endblock %}

{% block body %}
    <h1>Edit Logboek</h1>

    {{ include('logboek/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('logboek_index') }}">back to list</a>


    {% if is_granted('ROLE_ADMIN') %}
        {{ include('logboek/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
