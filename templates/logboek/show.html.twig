{% extends 'base.html.twig' %}

{% block title %}Logboek{% endblock %}

{% block body %}
    <h1>Logboek</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ logboek.id }}</td>
            </tr>
            <tr>
                <th>UserId</th>
                <td>{{ logboek.userId }}</td>
            </tr>
            <tr>
                <th>Brief_nr</th>
                <td>{{ logboek.briefNr }}</td>
            </tr>
            <tr>
                <th>Datum</th>
                <td>{{ logboek.datum ? logboek.datum|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>ChauffeurId</th>
                <td>{{ logboek.chauffeurId }}</td>
            </tr>
            <tr>
                <th>TrucId</th>
                <td>{{ logboek.trucId }}</td>
            </tr>
            <tr>
                <th>Kubs</th>
                <td>{{ logboek.kubs }}</td>
            </tr>
            <tr>
                <th>Laadplaats</th>
                <td>{{ logboek.laadplaats }}</td>
            </tr>
            <tr>
                <th>Vertrektijd</th>
                <td>{{ logboek.vertrektijd ? logboek.vertrektijd|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Bestemming</th>
                <td>{{ logboek.bestemming }}</td>
            </tr>
            <tr>
                <th>Evenement</th>
                <td>{{ logboek.evenement }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('logboek_index') }}">back to list</a>

    <a href="{{ path('logboek_edit', {'id': logboek.id}) }}">edit</a>

    {{ include('logboek/_delete_form.html.twig') }}
{% endblock %}
